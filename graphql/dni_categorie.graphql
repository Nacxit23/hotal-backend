type DniCategorie @model {
    id: ID! @globalId
    nameDni: String!
    dataDni: String!
    dniCategories: [DniCategorie!]! @hasMany(type:"connection")
}

extend type Query {
    dniCategories: [DniCategorie!]! @field(resolver: "App\\GraphQL\\Queries\\DniCategorieQuery@resolve")
}

type Room @model @model {
    id: ID! @globalId
    description: String
    location: String!
    nightPrice: Int!
    roomCategorie: RoomCategorie! @belongTo
    reservations: [Reservation!]! @hasMany(type: "connection")
}
